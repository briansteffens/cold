#include <stdio.h>
#include <stdlib.h>

#include "cold.h"
#include "interpreter.h"
#include "compiler.h"
#include "generator.h"
/*
int generate()
{
    struct PatternSet* patterns = load_patterns();

    if (!patterns)
    {
        printf("Failed to load patterns\n");
        return 1;
    }

    free_patterns(patterns);

    printf("yep\n");
}
*/
